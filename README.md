Jobly - Документация проекта
Интерактивный портал для соискателей и работодателей
О проекте

Jobly — современная платформа для поиска работы и сотрудников, разработанная с использованием React, TypeScript и Vite. Проект реализован по дизайну Figma с соблюдением всех принципов адаптивного веб-дизайна.

Макет Figma:
Открыть в Figma
Технологический стек: React + Vite
Преимущества выбранного стека

    ⚡ Молниеносная разработка - Vite обеспечивает мгновенный запуск сервера и горячую перезагрузку модулей (HMR)

    📦 Оптимизированные сборки - Продакшн-сборки на основе Rollup с эффективным деревом зависимостей

    🧩 Богатая экосистема - Доступ к огромному количеству библиотек React

    🔍 Типобезопасность - TypeScript предотвращает множество ошибок на этапе разработки

    🚀 Современные возможности - Поддержка ES-модулей, JSX и современных стандартов JavaScript

    🛠 Простая настройка - Минимальная конфигурация для начала работы

Особенности и сложности

    📚 Кривая обучения - Требует глубокого понимания React и концепций Vite

    🧪 Тестирование - Требует дополнительной настройки для комплексного тестирования

    📱 Мобильная разработка - Не предназначен для нативной мобильной разработки

    🔄 Горячее обновление - Иногда может требовать ручной перезагрузки при сложных изменениях

Структура проекта
text

src/
├── components/          # Реиспользуемые компоненты
│   ├── Header/          # Шапка сайта с модальным окном
│   ├── Footer/          # Подвал сайта
│   ├── Preloader/       # Индикатор загрузки
│   ├── Candidates/      # Компонент кандидатов
│   ├── Card/            # Карточка элемента
│   ├── CardList/        # Список карточек
│   ├── Place/           # Основной секции с контентом
│   ├── Connect/         # Компонент подключения
│   ├── Setup/           # Настройки
│   ├── Layout/          # Основной лейаут приложения
│   └── Message.tsx      # Компонент сообщений
│
├── pages/               # Страницы приложения
│   ├── HomePage.tsx     # Главная страница
│   ├── CardsPage.tsx    # Страница всех карточек (по умолчанию 10)
│   └── NotFoundPage.tsx # Страница 404
│
├── assets/              # Ресурсы
│   ├── styles/          # Глобальные стили
│   ├── images/          # Изображения
│   └── react.svg        # SVG-иконки
│
├── App.css              # Стили приложения
├── App.tsx              # Корневой компонент
├── index.css            # Глобальные стили
├── main.tsx             # Точка входа
└── vite-env.d.ts        # Объявления типов Vite

Ключевые особенности реализации

    Адаптивный дизайн
    Проект полностью адаптирован под различные устройства с тремя ключевыми контрольными точками:

        Мобильные устройства (360px)

        Планшеты (768px)

        Десктоп (1440px)

    Для реализации адаптивности используются CSS-медиазапросы и гибкая сетка Flexbox.

    Обработка неработающих элементов
    Все кнопки без реализованного функционала ведут на кастомную страницу 404 (NotFoundPage), что обеспечивает плавный пользовательский опыт вместо "битых" ссылок.

    Компонентный подход
    Приложение построено по модульному принципу с чётким разделением ответственности:

        Компоненты не зависят от внешних данных

        Логика и представление разделены

        Используются CSS-модули для изоляции стилей

    Маршрутизация
    Навигация между страницами реализована с помощью React Router, обеспечивая SPA-опыт без перезагрузки страниц.

Детали реализации
Слайдер Swiper

Для реализации интерактивного слайдера используется библиотека Swiper:
tsx

<Swiper
  modules={[Navigation, Pagination]}
  navigation
  pagination={{ clickable: true }}
  onSlideChange={(swiper) => setActiveIndex(swiper.realIndex)}
>
  {cards.map((card) => (
    <SwiperSlide key={card.id}>
      <img
        src={card.image}
        alt={card.title}
        loading="lazy"
      />
    </SwiperSlide>
  ))}
</Swiper>

Особенности реализации:

    Плавные анимации переходов

    Навигационные стрелки и индикаторы

    Ленивая загрузка изображений

    Синхронизация с активной карточкой

    Полная поддержка touch-жестов

Система карточек

Карточки — ключевой элемент интерфейса Jobly:
tsx

const Card: React.FC<CardProps> = React.memo(({ 
  title, 
  body, 
  active = false, 
  onClick 
}) => {
  return (
    <div className={`${styles.feature} ${active ? styles.active : ''}`}>
      <h3>{title}</h3>
      <p>{body}</p>
    </div>
  );
});

Особенности:

    Анимации при наведении

    Подсветка активного состояния

    Оптимизированная производительность с React.memo

    Адаптивный дизайн для всех устройств

Загрузка данных

Приложение получает данные карточек с удалённого API (JSONPlaceholder) с обработкой различных сценариев:
tsx

useEffect(() => {
  const fetchData = async () => {
    try {
      const response = await fetch(
        `https://jsonplaceholder.typicode.com/posts?_limit=${limit}`
      );
      
      if (!response.ok) {
        throw new Error(`Ошибка HTTP! Статус: ${response.status}`);
      }
      
      const data = await response.json() as ApiResponse[];
      // Обработка и преобразование данных
    } catch (err) {
      // Подробная обработка ошибок
      const error = err instanceof Error ? err : new Error(String(err));
      console.error("Ошибка загрузки:", error.message);
      setError(error.message);
      // Использование локальных данных
      setCards(DEFAULT_CARDS);
    }
  };
  
  fetchData();
}, [limit]);

Особенности реализации:

    Загрузка данных с внешнего API

    Детальная обработка ошибок сети

    Локальное кэширование данных

    Фолбэк на стандартные данные при ошибках

    Интеграция с VPN для доступа к заблокированным ресурсам

Архитектура сайта

Проект имеет продуманную структуру:

    Layout компонент - Единая обёртка для всех страниц

    Preloader - Индикатор загрузки для асинхронных операций

    Маршрутизация - Чистая навигация между страницами

    404 Страница - Кастомная страница для несуществующих маршрутов

    Глобальное состояние - Управление состоянием через React Context

Последние улучшения
🧠 Улучшения типизации

    Устранение any типов: Все компоненты теперь используют строгую типизацию

    Конкретные типы для Swiper: Добавлена точная типизация для работы со слайдером

    Типизация API-ответов: Чёткое определение структуры получаемых данных

    displayName для компонентов: Улучшена отладка в React DevTools

tsx

// Было
const swiperRef = useRef<any>(null);

// Стало
const swiperRef = useRef<SwiperType | null>(null);

⚡ Оптимизация производительности

    React.memo для карточек: Предотвращение лишних ререндеров

    useCallback для обработчиков: Стабильные ссылки на функции

    Удаление неиспользуемых пропсов: Очистка интерфейсов компонентов

    Ленивая загрузка изображений: Ускорение первоначальной загрузки

tsx

const Card = React.memo((props) => { /* ... */ });

const handleClick = useCallback(() => { /* ... */ }, []);

🛡 Улучшенная обработка ошибок

    Проверка статуса HTTP: Контроль успешности запросов

    Детализированные сообщения: Понятные пользователю уведомления

    Резервные данные: Использование локальных данных при сбоях

    Логирование ошибок: Запись в консоль для отладки

♿ Доступность (a11y)

    ARIA-атрибуты: Улучшение семантики для скринридеров

    Клавиатурная навигация: Поддержка управления с клавиатуры

    Осмысленные alt-тексты: Описания изображений для доступности

    Фокус-менеджмент: Корректное управление фокусом элементов

tsx

<div
  role="button"
  tabIndex={0}
  aria-label={`Выбрать ${card.title}`}
  onKeyDown={(e) => e.key === 'Enter' && handleClick()}
>
  {/* Содержимое карточки */}
</div>

✨ Качество кода

    Удаление неиспользуемого кода: Очистка от мертвого кода

    Улучшенная структура: Чёткое разделение ответственности

    Семантические комментарии: Пояснения для сложных участков

    Единые стандарты именования: Консистентность по всему проекту

🔒 Безопасность

    Валидация параметров URL: Защита от невалидных значений

    Обработка граничных случаев: Защита от неожиданных сценариев

    Безопасное приведение типов: Предотвращение runtime-ошибок

    Экранирование данных: Защита от XSS-атак
